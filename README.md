
  # Strong Start App (Copy)

  This is a code bundle for Strong Start App (Copy). The original project is available at https://www.figma.com/design/pCS8gNJB4AM5ZLngR8oz8m/Strong-Start-App--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  